The issue was resolved after correcting the GRUB boot parameters related to console settings and updating the /etc/fstab configuration. Once these changes were applied, the initramfs was regenerated and the GRUB configuration was rebuilt. After rebooting the VM, the system started normally without the black screen issue. The root cause was therefore linked to incorrect boot console parameters and an inconsistent filesystem configuration, which prevented the operating system from completing the boot process successfully.