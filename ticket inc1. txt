Assignment group: VMware / Virtualization Team

We now have strong evidence of a storage bottleneck:
- load average >100 while CPU remains mostly idle
- several tasks blocked in D state
- disks/utilization close to saturation
- even lsof hangs when checking RPM/DNF locks

This confirms the patch timeout is a consequence of storage I/O contention on the underlying VMware datastore.


Issue:
Patching job failure due to system unresponsiveness.

Analysis:
We identified a severe I/O bottleneck on the server.

Evidence:
- Load average >100 with CPU idle ~94%
- Multiple processes in D state (blocked on I/O)
- Disk utilization close to 100%
- lsof command hanging due to kernel I/O wait

Conclusion:
The issue is related to storage latency / contention on the underlying VMware datastore.

Request:
Please investigate datastore latency, I/O contention, and queue saturation on the VM.